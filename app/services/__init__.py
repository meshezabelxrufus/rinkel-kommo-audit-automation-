"""
Services — business logic layer.

Each service encapsulates a single domain concern:
- call_service.py     → call record lifecycle
- transcription.py    → Whisper transcription orchestration
- export_service.py   → JSONL export for Claude auditing
"""
